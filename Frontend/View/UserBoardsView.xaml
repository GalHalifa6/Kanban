<Window x:Class="Frontend.View.UserBoardsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Frontend.View"
        mc:Ignorable="d"
        Title="BoardView" Height="450" Width="800" Loaded="Window_Loaded">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="598*"/>
            <ColumnDefinition Width="25*"/>
            <ColumnDefinition Width="35*"/>
            <ColumnDefinition Width="142*"/>
        </Grid.ColumnDefinitions>
        <ListBox x:Name="UserBoards" ItemsSource="{Binding Path = user.Boards }" SelectionChanged="ListBox_SelectionChanged" Margin="10,21,34,127" Grid.ColumnSpan="3"/>

    </Grid>
</Window>
